import { Request, Response } from "express";
// import WeatherModel from "../../models/weather/WeatherModel";

const getWeather = (_req: Request, res: Response) => {



};

export default module.exports = {
    getWeather
};